# The left half is the split peripheral, so central-only USB and smooth
# scrolling options from the shared Crosses shield config must be disabled here.
CONFIG_ZMK_USB=n
CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=n
CONFIG_PMW3610_REPORT_INTERVAL_MIN=8
